info:
  name: register
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/user/create"
  body:
    type: json
    data: |-
      {
        "userName": "karan11",
        "userPassword": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
