info:
  name: login user
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/user/loginJwt"
  body:
    type: json
    data: |+
      {
        "userName": "super2",
        "userPassword": "123456"
      }


  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
