info:
  name: login admin
  type: http
  seq: 11

http:
  method: POST
  url: "{{baseUrl}}/user/loginJwt"
  body:
    type: json
    data: |-
      {
        "userName": "admin",
        "userPassword": "admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
