info:
  name: register superuser
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/user/create"
  body:
    type: json
    data: |+
      {
        "userName": "super1",
        "userPassword": "123456",
        "role": "superuser"
      }


  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
